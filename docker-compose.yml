services:
  dynamodb:
    # Published AWS image tag verified 2026-09-19; intentionally no floating latest.
    image: amazon/dynamodb-local:3.3.1
    command: ["-jar", "DynamoDBLocal.jar", "-sharedDb", "-inMemory"]
    working_dir: /home/dynamodblocal
    ports:
      - "127.0.0.1:8000:8000"
